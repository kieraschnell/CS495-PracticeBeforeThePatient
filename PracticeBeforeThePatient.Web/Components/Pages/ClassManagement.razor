@page "/class-management"
@rendermode InteractiveServer
@using System.Net.Http.Json
@using System.Globalization
@using PracticeBeforeThePatient.Web.Services
@inject ApiClient Api

<PageTitle>Class Management</PageTitle>

@if (!string.IsNullOrWhiteSpace(_message))
{
    <div @key="_messageVersion" class="saveMessage overlayToast @_messageKind" role="status" aria-live="polite">
        @_message
    </div>
}

@if (!_isTeacherLoaded)
{
    <div class="card">
        <div class="sectionTitle">Loading</div>
        <div class="narrLine">Checking access</div>
    </div>
    return;
}

@if (!_isTeacher)
{
    <div class="card">
        <div class="sectionTitle">Not available</div>
        <div class="narrLine">This page is only available to teachers and admins</div>
    </div>
    return;
}

<div class="simShell">
    <div class="simHeaderRow">
        <div class="simHeaderText">
            <div class="simBadge">Teacher</div>
            <div class="simTitle">Class Management</div>
            <div class="simSubtitle">Create classes, assign scenarios, and open grading pages</div>
        </div>
        <button class="primaryBtn" type="button" @onclick="OpenAddClassModal">Add class</button>
    </div>

    <div class="card" @onclick="CloseMenu">
        <div class="sectionTitle">Classes</div>

        <div class="editArea">
            <div class="editTopRow">
                <div class="editSelect">
                    <label class="editorLabel">Select a class</label>
                    <select class="editorInput" value="@_selectedClassId" @onchange="OnSelectedClassChanged">
                        <option value="0">Select</option>
                        @foreach (var c in _classes)
                        {
                            <option value="@c.Id">@c.Name</option>
                        }
                    </select>
                </div>

                <div class="kebabWrap" @onclick:stopPropagation="true">
                    <button class="kebabBtn" @onclick="ToggleMenu" disabled="@(_selectedClassId == 0)">&#8942;</button>

                    @if (_menuOpen && _selectedClassId != 0)
                    {
                        <div class="kebabMenu">
                            <button class="kebabMenuItem danger" @onclick="DeleteSelectedClassAsync">Delete class</button>
                        </div>
                    }
                </div>
            </div>

            @if (_selectedClass is not null)
            {
                <div class="divider"></div>

                <div class="assignmentTopRow">
                    <div class="sectionTitle">Assignments</div>
                    <button class="secondaryBtn" type="button" @onclick="OpenAddAssignmentModalAsync">Add assignment</button>
                </div>

                @if (_assignments.Count == 0)
                {
                    <div class="infoMessage">No assignments yet for this class</div>
                }
                else
                {
                    <div class="assignmentList">
                        @foreach (var a in _assignments)
                        {
                            var submitted = a.Submissions.Count(x => x.SubmittedAtUtc.HasValue);
                            var total = _selectedClass.Students.Count;
                            <div class="assignmentItem">
                                <div class="assignmentInfo">
                                    <div class="assignmentTitle">@a.Name</div>
                                    <div class="assignmentMeta">Scenario: @a.ScenarioId</div>
                                    <div class="assignmentMeta">Assign at: @FormatAssignedAtCst(a.AssignedAtUtc)</div>
                                    <div class="assignmentMeta">
                                        Due: @FormatDueAtCst(a.DueAtUtc)
                                    </div>
                                    <div class="assignmentMeta">Submitted: @submitted / @total</div>
                                </div>
                                <div class="assignmentActions">
                                    <button class="secondaryBtn" type="button" @onclick="() => OpenEditAssignmentModalAsync(a)">Edit assignment</button>
                                    <a class="secondaryBtn" href="@($"assignment-grading/{_selectedClass.Id}/{a.Id}")">Grade</a>
                                    <button class="deleteBtn" @onclick="() => DeleteAssignmentAsync(a.Id)">Delete</button>
                                </div>
                            </div>
                        }
                    </div>
                }

                <div class="divider"></div>

                <div class="assignmentTopRow">
                    <div class="sectionTitle">Manage Teachers</div>
                    <button class="secondaryBtn" type="button" @onclick="OpenAddTeacherModal">Add teacher</button>
                </div>

                <div class="divider"></div>

                <div class="classMgmtRosterHeader">
                    <div class="panel-title">Teachers</div>
                    <div class="classMgmtCount">@_selectedClass!.Teachers.Count teacher(s)</div>
                </div>

                @if (_selectedClass!.Teachers.Count == 0)
                {
                    <div class="infoMessage">No teachers yet</div>
                }
                else
                {
                    <div class="classMgmtRoster">
                        @foreach (var email in _selectedClass!.Teachers.OrderBy(x => x))
                        {
                            <div class="classMgmtStudent">
                                <div class="classMgmtStudentEmail">@email</div>
                                <button class="deleteBtn" @onclick="() => RemoveTeacherAsync(email)" disabled="@IsCurrentTeacher(email)">Remove</button>
                            </div>
                        }
                    </div>
                }

                <div class="divider"></div>

                <div class="assignmentTopRow">
                    <div class="sectionTitle">Manage Students</div>
                    <button class="secondaryBtn" type="button" @onclick="OpenAddStudentModal">Add student</button>
                </div>

                <div class="divider"></div>

                <div class="classMgmtRosterHeader">
                    <div class="panel-title">Roster</div>
                    <div class="classMgmtCount">@_selectedClass!.Students.Count student(s)</div>
                </div>

                @if (_selectedClass!.Students.Count == 0)
                {
                    <div class="infoMessage">No students yet</div>
                }
                else
                {
                    <div class="classMgmtRoster">
                        @foreach (var email in _selectedClass!.Students.OrderBy(x => x))
                        {
                            <div class="classMgmtStudent">
                                <div class="classMgmtStudentEmail">@email</div>
                                <button class="deleteBtn" @onclick="() => RemoveStudentAsync(email)">Remove</button>
                            </div>
                        }
                    </div>
                }
            }
            </div>
        </div>
    </div>

@if (_isAddClassModalOpen)
{
    <div class="classMgmtModalBackdrop" @onclick="CloseAddClassModal">
        <div class="classMgmtModalCard classMgmtModalCardNarrow classMgmtModalCardLight" @onclick:stopPropagation="true">
            <div class="classMgmtModalTop">
                <div class="sectionTitle">Add class</div>
                <button class="secondaryBtn" type="button" @onclick="CloseAddClassModal">Cancel</button>
            </div>

            <div class="createArea">
                <label class="editorLabel">Class name</label>
                <div class="createRow">
                    <input class="editorInput"
                           value="@_newClassName"
                           @oninput="OnNewClassNameInput"
                           @onkeydown="OnNewClassNameKeyDown"
                           placeholder="Example, AT 301 Lab Section A" />
                    <button class="primaryBtn" type="button" @onclick="CreateClassAsync" disabled="@string.IsNullOrWhiteSpace(_newClassName)">Create</button>
                </div>
                <div class="createHint">Class names must be unique</div>
            </div>
        </div>
    </div>
}

@if (_selectedClass is not null && _isAddTeacherModalOpen)
{
    <div class="classMgmtModalBackdrop" @onclick="CloseAddTeacherModal">
        <div class="classMgmtModalCard classMgmtModalCardNarrow" @onclick:stopPropagation="true">
            <div class="classMgmtModalTop">
                <div class="sectionTitle">Add teacher</div>
                <button class="secondaryBtn" type="button" @onclick="CloseAddTeacherModal">Cancel</button>
            </div>

            <div class="createArea">
                <label class="editorLabel">Teacher email</label>
                <div class="createRow">
                    <input class="editorInput"
                           value="@_teacherEmailToAdd"
                           @oninput="OnTeacherEmailInput"
                           @onkeydown="OnAddTeacherKeyDown"
                           placeholder="teacher@ua.edu" />
                    <button class="primaryBtn" type="button" @onclick="AddTeacherAsync" disabled="@string.IsNullOrWhiteSpace(_teacherEmailToAdd)">Add</button>
                </div>
                <div class="createHint">Adds a teacher to this class only</div>
            </div>
        </div>
    </div>
}

@if (_selectedClass is not null && _isAddStudentModalOpen)
{
    <div class="classMgmtModalBackdrop" @onclick="CloseAddStudentModal">
        <div class="classMgmtModalCard classMgmtModalCardNarrow" @onclick:stopPropagation="true">
            <div class="classMgmtModalTop">
                <div class="sectionTitle">Add student</div>
                <button class="secondaryBtn" type="button" @onclick="CloseAddStudentModal">Cancel</button>
            </div>

            <div class="createArea">
                <label class="editorLabel">Student email</label>
                <div class="createRow">
                    <input class="editorInput"
                           value="@_emailToAdd"
                           @oninput="OnEmailInput"
                           @onkeydown="OnAddStudentKeyDown"
                           placeholder="student@ua.edu" />
                    <button class="primaryBtn" type="button" @onclick="AddStudentAsync" disabled="@string.IsNullOrWhiteSpace(_emailToAdd)">Add</button>
                </div>
                <div class="createHint">Adds a student roster entry for this class</div>
            </div>
        </div>
    </div>
}

@if (_selectedClass is not null && _isAddAssignmentModalOpen)
{
    <div class="classMgmtModalBackdrop" @onclick="CloseAddAssignmentModal">
        <div class="classMgmtModalCard classMgmtModalCardLight" @onclick:stopPropagation="true">
            <div class="classMgmtModalTop">
                <div class="sectionTitle">Add assignment</div>
                <button class="secondaryBtn" type="button" @onclick="CloseAddAssignmentModal">Cancel</button>
            </div>

            @if (_isLoadingScenarios)
            {
                <div class="infoMessage">Loading scenarios</div>
            }
            else
            {
                @if (!string.IsNullOrWhiteSpace(_assignmentModalMessage))
                {
                    <div class="warningMessage assignmentModalMessage">@_assignmentModalMessage</div>
                }

                <div class="assignmentModalForm">
                    <div>
                        <label class="editorLabel">Assignment name <span class="requiredMark">*</span></label>
                        <input class="editorInput"
                               type="text"
                               @bind="_assignmentName"
                               @bind:event="oninput"
                               placeholder="Example: Week 4 Chest Pain Case" />
                    </div>
                    <div>
                        <label class="editorLabel">Scenario <span class="requiredMark">*</span></label>
                        <select class="editorInput" @bind="_assignmentScenarioId">
                            <option value="">Select</option>
                            @foreach (var id in _allScenarioIds)
                            {
                                <option value="@id">@id</option>
                            }
                        </select>
                    </div>
                    <div>
                        <label class="editorLabel">Assign date (optional)</label>
                        <input class="editorInput"
                               type="date"
                               value="@_assignmentAssignDate"
                               @onchange="OnAssignmentAssignDateChanged" />
                    </div>
                    <div>
                        <label class="editorLabel">Assign time (optional)</label>
                        <div style="display:grid; grid-template-columns:5rem auto 5rem 5rem; column-gap:8px; align-items:center; justify-content:start;">
                            <input class="editorInput timeNumberInput"
                                   style="width:5rem; min-width:5rem;"
                                   type="text"
                                   inputmode="numeric"
                                   maxlength="2"
                                   disabled="@string.IsNullOrWhiteSpace(_assignmentAssignDate)"
                                   value="@_assignmentAssignHour"
                                   @oninput="OnAssignmentAssignHourChanged" />
                            <span class="timeSeparator" style="font-size:22px; font-weight:700; line-height:1; text-align:center;">:</span>
                            <input class="editorInput timeNumberInput"
                                   style="width:5rem; min-width:5rem;"
                                   type="text"
                                   inputmode="numeric"
                                   maxlength="2"
                                   disabled="@string.IsNullOrWhiteSpace(_assignmentAssignDate)"
                                   value="@_assignmentAssignMinute"
                                   @oninput="OnAssignmentAssignMinuteChanged" />
                            <select class="editorInput timePeriodInput"
                                    style="width:5rem; min-width:5rem;"
                                    disabled="@string.IsNullOrWhiteSpace(_assignmentAssignDate)"
                                    @bind="_assignmentAssignPeriod">
                                @foreach (var period in MeridiemOptions)
                                {
                                    <option value="@period">@period</option>
                                }
                            </select>
                        </div>
                    </div>
                    <div>
                        <label class="editorLabel">Due date (optional)</label>
                        <input class="editorInput"
                               type="date"
                               value="@_assignmentDate"
                               @onchange="OnAssignmentDateChanged" />
                    </div>
                    <div>
                        <label class="editorLabel">Due time (optional)</label>
                        <div style="display:grid; grid-template-columns:5rem auto 5rem 5rem; column-gap:8px; align-items:center; justify-content:start;">
                            <input class="editorInput timeNumberInput"
                                   style="width:5rem; min-width:5rem;"
                                   type="text"
                                   inputmode="numeric"
                                   maxlength="2"
                                   disabled="@string.IsNullOrWhiteSpace(_assignmentDate)"
                                   value="@_assignmentDueHour"
                                   @oninput="OnAssignmentDueHourChanged" />
                            <span class="timeSeparator" style="font-size:22px; font-weight:700; line-height:1; text-align:center;">:</span>
                            <input class="editorInput timeNumberInput"
                                   style="width:5rem; min-width:5rem;"
                                   type="text"
                                   inputmode="numeric"
                                   maxlength="2"
                                   disabled="@string.IsNullOrWhiteSpace(_assignmentDate)"
                                   value="@_assignmentDueMinute"
                                   @oninput="OnAssignmentDueMinuteChanged" />
                            <select class="editorInput timePeriodInput"
                                    style="width:5rem; min-width:5rem;"
                                    disabled="@string.IsNullOrWhiteSpace(_assignmentDate)"
                                    @bind="_assignmentDuePeriod">
                                @foreach (var period in MeridiemOptions)
                                {
                                    <option value="@period">@period</option>
                                }
                            </select>
                        </div>
                    </div>
                </div>

                <div class="assignmentModalActions">
                    <button class="primaryBtn" type="button" @onclick="CreateAssignmentAsync">Assign scenario</button>
                </div>
            }
        </div>
    </div>
}

@if (_selectedClass is not null && _editingAssignmentId.HasValue)
{
    var editingAssignment = _assignments.FirstOrDefault(x => x.Id == _editingAssignmentId.Value);
    if (editingAssignment is not null)
    {
        <div class="classMgmtModalBackdrop" @onclick="CloseEditAssignmentModal">
            <div class="classMgmtModalCard classMgmtModalCardLight" @onclick:stopPropagation="true">
                <div class="classMgmtModalTop">
                    <div class="sectionTitle">Edit assignment</div>
                    <button class="secondaryBtn" type="button" @onclick="CloseEditAssignmentModal" disabled="@_isSavingAssignment">Cancel</button>
                </div>

                <div class="assignmentMeta">@editingAssignment.Name · @editingAssignment.ScenarioId</div>

                @if (!string.IsNullOrWhiteSpace(_assignmentModalMessage))
                {
                    <div class="warningMessage assignmentModalMessage">@_assignmentModalMessage</div>
                }

                <div class="assignmentModalForm">
                    <div>
                        <label class="editorLabel">Assignment name <span class="requiredMark">*</span></label>
                        <input class="editorInput"
                               type="text"
                               @bind="_editAssignmentName"
                               @bind:event="oninput"
                               placeholder="Example: Week 4 Chest Pain Case" />
                    </div>
                    <div>
                        <label class="editorLabel">Scenario <span class="requiredMark">*</span></label>
                        <select class="editorInput" @bind="_editAssignmentScenarioId">
                            <option value="">Select</option>
                            @foreach (var id in _allScenarioIds)
                            {
                                <option value="@id">@id</option>
                            }
                        </select>
                    </div>
                    <div>
                        <label class="editorLabel">Assign date (optional)</label>
                        <input class="editorInput"
                               type="date"
                               value="@_editAssignmentAssignDate"
                               @onchange="OnEditAssignmentAssignDateChanged" />
                    </div>
                    <div>
                        <label class="editorLabel">Assign time (optional)</label>
                        <div style="display:grid; grid-template-columns:5rem auto 5rem 5rem; column-gap:8px; align-items:center; justify-content:start;">
                            <input class="editorInput timeNumberInput"
                                   style="width:5rem; min-width:5rem;"
                                   type="text"
                                   inputmode="numeric"
                                   maxlength="2"
                                   disabled="@string.IsNullOrWhiteSpace(_editAssignmentAssignDate)"
                                   value="@_editAssignmentAssignHour"
                                   @oninput="OnEditAssignmentAssignHourChanged" />
                            <span class="timeSeparator" style="font-size:22px; font-weight:700; line-height:1; text-align:center;">:</span>
                            <input class="editorInput timeNumberInput"
                                   style="width:5rem; min-width:5rem;"
                                   type="text"
                                   inputmode="numeric"
                                   maxlength="2"
                                   disabled="@string.IsNullOrWhiteSpace(_editAssignmentAssignDate)"
                                   value="@_editAssignmentAssignMinute"
                                   @oninput="OnEditAssignmentAssignMinuteChanged" />
                            <select class="editorInput timePeriodInput"
                                    style="width:5rem; min-width:5rem;"
                                    disabled="@string.IsNullOrWhiteSpace(_editAssignmentAssignDate)"
                                    @bind="_editAssignmentAssignPeriod">
                                @foreach (var period in MeridiemOptions)
                                {
                                    <option value="@period">@period</option>
                                }
                            </select>
                        </div>
                    </div>
                    <div>
                        <label class="editorLabel">Due date (optional)</label>
                        <input class="editorInput"
                               type="date"
                               value="@_editAssignmentDueDate"
                               @onchange="OnEditAssignmentDueDateChanged" />
                    </div>
                    <div>
                        <label class="editorLabel">Due time (optional)</label>
                        <div style="display:grid; grid-template-columns:5rem auto 5rem 5rem; column-gap:8px; align-items:center; justify-content:start;">
                            <input class="editorInput timeNumberInput"
                                   style="width:5rem; min-width:5rem;"
                                   type="text"
                                   inputmode="numeric"
                                   maxlength="2"
                                   disabled="@string.IsNullOrWhiteSpace(_editAssignmentDueDate)"
                                   value="@_editAssignmentDueHour"
                                   @oninput="OnEditAssignmentDueHourChanged" />
                            <span class="timeSeparator" style="font-size:22px; font-weight:700; line-height:1; text-align:center;">:</span>
                            <input class="editorInput timeNumberInput"
                                   style="width:5rem; min-width:5rem;"
                                   type="text"
                                   inputmode="numeric"
                                   maxlength="2"
                                   disabled="@string.IsNullOrWhiteSpace(_editAssignmentDueDate)"
                                   value="@_editAssignmentDueMinute"
                                   @oninput="OnEditAssignmentDueMinuteChanged" />
                            <select class="editorInput timePeriodInput"
                                    style="width:5rem; min-width:5rem;"
                                    disabled="@string.IsNullOrWhiteSpace(_editAssignmentDueDate)"
                                    @bind="_editAssignmentDuePeriod">
                                @foreach (var period in MeridiemOptions)
                                {
                                    <option value="@period">@period</option>
                                }
                            </select>
                        </div>
                    </div>
                </div>

                <div class="assignmentModalActions assignmentDueModalActions">
                    <button class="primaryBtn"
                            type="button"
                            @onclick="() => SaveAssignmentAsync(editingAssignment.Id)"
                            disabled="@_isSavingAssignment">
                        Save assignment
                    </button>
                </div>
            </div>
        </div>
    }
}

@code {
    private List<ClassRosterDto> _classes = new();
    private string _newClassName = "";
    private bool _isAddClassModalOpen;
    private bool _isAddTeacherModalOpen;
    private bool _isAddStudentModalOpen;
    private int _selectedClassId;
    private ClassRosterDto? _selectedClass;
    private string _emailToAdd = "";
    private string _teacherEmailToAdd = "";
    private string _currentEmail = "";

    private string _message = "";
    private string _messageKind = "success";
    private int _messageVersion;
    private string _assignmentModalMessage = "";

    private bool _menuOpen;

    private bool _isLoadingScenarios;
    private List<string> _allScenarioIds = new();

    private List<ClassAssignmentDto> _assignments = new();
    private bool _isAddAssignmentModalOpen;
    private string _assignmentName = "";
    private string _assignmentScenarioId = "";
    private string _assignmentAssignDate = "";
    private string _assignmentAssignHour = "";
    private string _assignmentAssignMinute = "";
    private string _assignmentAssignPeriod = DefaultAssignPeriod;
    private string _assignmentDate = "";
    private string _assignmentDueHour = "";
    private string _assignmentDueMinute = "";
    private string _assignmentDuePeriod = DefaultDuePeriod;
    private int? _editingAssignmentId;
    private string _editAssignmentName = "";
    private string _editAssignmentScenarioId = "";
    private string _editAssignmentAssignDate = "";
    private string _editAssignmentAssignHour = "";
    private string _editAssignmentAssignMinute = "";
    private string _editAssignmentAssignPeriod = DefaultAssignPeriod;
    private string _editAssignmentDueDate = "";
    private string _editAssignmentDueHour = "";
    private string _editAssignmentDueMinute = "";
    private string _editAssignmentDuePeriod = DefaultDuePeriod;
    private bool _isSavingAssignment;
    private static readonly TimeSpan CstOffset = TimeSpan.FromHours(-6);
    private const string DefaultAssignHour = "12";
    private const string DefaultAssignMinute = "00";
    private const string DefaultAssignPeriod = "AM";
    private const string DefaultDueHour = "11";
    private const string DefaultDueMinute = "59";
    private const string DefaultDuePeriod = "PM";
    private static readonly string[] MeridiemOptions = ["AM", "PM"];
    private bool _isTeacherLoaded;
    private bool _isTeacher;

    protected override async Task OnInitializedAsync()
    {
        var access = await Api.GetAccessAsync();
        _isTeacher = access?.IsTeacher == true;
        _currentEmail = (access?.Email ?? "").Trim().ToLowerInvariant();
        _isTeacherLoaded = true;

        if (_isTeacher)
        {
            await ReloadClassesAsync();
            if (_classes.Count > 0)
            {
                _selectedClassId = _classes[0].Id;
                _selectedClass = _classes[0];
                await ReloadAvailableScenariosAsync();
                await ReloadAssignmentsAsync();
            }
        }
    }

    private void OpenAddClassModal()
    {
        _isAddClassModalOpen = true;
        _isAddAssignmentModalOpen = false;
        _isAddTeacherModalOpen = false;
        _isAddStudentModalOpen = false;
        CloseEditAssignmentModal();
    }

    private void CloseAddClassModal()
    {
        _isAddClassModalOpen = false;
    }

    private void ToggleMenu()
    {
        _menuOpen = !_menuOpen;
    }

    private void CloseMenu()
    {
        _menuOpen = false;
    }

    private async Task OpenAddAssignmentModalAsync()
    {
        if (_selectedClass is null)
        {
            return;
        }

        ClearAssignmentModalMessage();
        await ReloadAvailableScenariosAsync();
        _isAddAssignmentModalOpen = true;
        _isAddClassModalOpen = false;
        _isAddTeacherModalOpen = false;
        _isAddStudentModalOpen = false;
        CloseEditAssignmentModal();
    }

    private void CloseAddAssignmentModal()
    {
        ClearAssignmentModalMessage();
        _isAddAssignmentModalOpen = false;
    }

    private void OpenAddTeacherModal()
    {
        _teacherEmailToAdd = "";
        _isAddTeacherModalOpen = true;
        _isAddStudentModalOpen = false;
        _isAddAssignmentModalOpen = false;
        _isAddClassModalOpen = false;
        CloseEditAssignmentModal();
    }

    private void CloseAddTeacherModal()
    {
        _isAddTeacherModalOpen = false;
    }

    private void OpenAddStudentModal()
    {
        _emailToAdd = "";
        _isAddStudentModalOpen = true;
        _isAddTeacherModalOpen = false;
        _isAddAssignmentModalOpen = false;
        _isAddClassModalOpen = false;
        CloseEditAssignmentModal();
    }

    private void CloseAddStudentModal()
    {
        _isAddStudentModalOpen = false;
    }

    private void SetMessage(string text, bool ok)
    {
        _message = text;
        _messageKind = ok ? "success" : "error";
        StateHasChanged();
        var version = ++_messageVersion;
        _ = ClearMessageLater(version);
    }

    private void SetAssignmentModalMessage(string text)
    {
        _assignmentModalMessage = text;
        StateHasChanged();
    }

    private void ClearAssignmentModalMessage()
    {
        _assignmentModalMessage = "";
    }

    private async Task ClearMessageLater(int version)
    {
        await Task.Delay(3000);
        if (version != _messageVersion)
        {
            return;
        }

        _message = "";
        await InvokeAsync(StateHasChanged);
    }

    private void OnNewClassNameInput(ChangeEventArgs e)
    {
        _newClassName = (e.Value?.ToString() ?? "").Trim();
    }

    private void OnEmailInput(ChangeEventArgs e)
    {
        _emailToAdd = (e.Value?.ToString() ?? "").Trim();
    }

    private void OnTeacherEmailInput(ChangeEventArgs e)
    {
        _teacherEmailToAdd = (e.Value?.ToString() ?? "").Trim();
    }

    private void OnAssignmentDateChanged(ChangeEventArgs e)
    {
        _assignmentDate = (e.Value?.ToString() ?? "").Trim();
        if (string.IsNullOrWhiteSpace(_assignmentDate))
        {
            _assignmentDueHour = "";
            _assignmentDueMinute = "";
            _assignmentDuePeriod = DefaultDuePeriod;
            return;
        }

        if (string.IsNullOrWhiteSpace(_assignmentDueHour))
        {
            _assignmentDueHour = DefaultDueHour;
        }

        if (string.IsNullOrWhiteSpace(_assignmentDueMinute))
        {
            _assignmentDueMinute = DefaultDueMinute;
        }
    }

    private void OnAssignmentDueHourChanged(ChangeEventArgs e)
    {
        _assignmentDueHour = NormalizeTimePartInput(e.Value?.ToString());
    }

    private void OnAssignmentDueMinuteChanged(ChangeEventArgs e)
    {
        _assignmentDueMinute = NormalizeTimePartInput(e.Value?.ToString());
    }

    private void OnEditAssignmentAssignDateChanged(ChangeEventArgs e)
    {
        _editAssignmentAssignDate = (e.Value?.ToString() ?? "").Trim();
        if (string.IsNullOrWhiteSpace(_editAssignmentAssignDate))
        {
            _editAssignmentAssignHour = "";
            _editAssignmentAssignMinute = "";
            _editAssignmentAssignPeriod = DefaultAssignPeriod;
            return;
        }

        if (string.IsNullOrWhiteSpace(_editAssignmentAssignHour))
        {
            _editAssignmentAssignHour = DefaultAssignHour;
        }

        if (string.IsNullOrWhiteSpace(_editAssignmentAssignMinute))
        {
            _editAssignmentAssignMinute = DefaultAssignMinute;
        }
    }

    private void OnEditAssignmentAssignHourChanged(ChangeEventArgs e)
    {
        _editAssignmentAssignHour = NormalizeTimePartInput(e.Value?.ToString());
    }

    private void OnEditAssignmentAssignMinuteChanged(ChangeEventArgs e)
    {
        _editAssignmentAssignMinute = NormalizeTimePartInput(e.Value?.ToString());
    }

    private void OnEditAssignmentDueDateChanged(ChangeEventArgs e)
    {
        _editAssignmentDueDate = (e.Value?.ToString() ?? "").Trim();
        if (string.IsNullOrWhiteSpace(_editAssignmentDueDate))
        {
            _editAssignmentDueHour = "";
            _editAssignmentDueMinute = "";
            _editAssignmentDuePeriod = DefaultDuePeriod;
            return;
        }

        if (string.IsNullOrWhiteSpace(_editAssignmentDueHour))
        {
            _editAssignmentDueHour = DefaultDueHour;
        }

        if (string.IsNullOrWhiteSpace(_editAssignmentDueMinute))
        {
            _editAssignmentDueMinute = DefaultDueMinute;
        }
    }

    private void OnEditAssignmentDueHourChanged(ChangeEventArgs e)
    {
        _editAssignmentDueHour = NormalizeTimePartInput(e.Value?.ToString());
    }

    private void OnEditAssignmentDueMinuteChanged(ChangeEventArgs e)
    {
        _editAssignmentDueMinute = NormalizeTimePartInput(e.Value?.ToString());
    }

    private void OnAssignmentAssignDateChanged(ChangeEventArgs e)
    {
        _assignmentAssignDate = (e.Value?.ToString() ?? "").Trim();
        if (string.IsNullOrWhiteSpace(_assignmentAssignDate))
        {
            _assignmentAssignHour = "";
            _assignmentAssignMinute = "";
            _assignmentAssignPeriod = DefaultAssignPeriod;
            return;
        }

        if (string.IsNullOrWhiteSpace(_assignmentAssignHour))
        {
            _assignmentAssignHour = DefaultAssignHour;
        }

        if (string.IsNullOrWhiteSpace(_assignmentAssignMinute))
        {
            _assignmentAssignMinute = DefaultAssignMinute;
        }
    }

    private void OnAssignmentAssignHourChanged(ChangeEventArgs e)
    {
        _assignmentAssignHour = NormalizeTimePartInput(e.Value?.ToString());
    }

    private void OnAssignmentAssignMinuteChanged(ChangeEventArgs e)
    {
        _assignmentAssignMinute = NormalizeTimePartInput(e.Value?.ToString());
    }

    private async Task OnNewClassNameKeyDown(KeyboardEventArgs e)
    {
        if (e.Key == "Enter")
        {
            await CreateClassAsync();
        }
    }

    private async Task OnAddStudentKeyDown(KeyboardEventArgs e)
    {
        if (e.Key == "Enter")
        {
            await AddStudentAsync();
        }
    }

    private async Task OnAddTeacherKeyDown(KeyboardEventArgs e)
    {
        if (e.Key == "Enter")
        {
            await AddTeacherAsync();
        }
    }

    private async Task ReloadClassesAsync()
    {
        try
        {
            var data = await Api.Http.GetFromJsonAsync<List<ClassRosterDto>>("api/classes");
            _classes = data ?? new List<ClassRosterDto>();
            _selectedClass = _classes.FirstOrDefault(x => x.Id == _selectedClassId);
        }
        catch
        {
            _classes = new List<ClassRosterDto>();
            _selectedClass = null;
            SetMessage("Could not load classes from the API", false);
        }
    }

    private async Task ReloadAvailableScenariosAsync()
    {
        if (_selectedClass is null)
        {
            _allScenarioIds = new List<string>();
            return;
        }

        _isLoadingScenarios = true;

        try
        {
            var all = await Api.Http.GetFromJsonAsync<List<string>>("api/scenarios");
            _allScenarioIds = (all ?? new List<string>())
                .OrderBy(x => x, StringComparer.OrdinalIgnoreCase)
                .ToList();

            if (string.IsNullOrWhiteSpace(_assignmentScenarioId) && _allScenarioIds.Count > 0)
            {
                _assignmentScenarioId = _allScenarioIds[0];
            }
        }
        catch
        {
            _allScenarioIds = new List<string>();
            SetMessage("Could not load scenarios from the API", false);
        }
        finally
        {
            _isLoadingScenarios = false;
        }
    }

    private async Task ReloadAssignmentsAsync()
    {
        if (_selectedClass is null)
        {
            ResetAssignmentState();
            return;
        }

        try
        {
            var assignments = await Api.Http.GetFromJsonAsync<List<ClassAssignmentDto>>($"api/classes/{_selectedClass.Id}/assignments");
            _assignments = (assignments ?? new List<ClassAssignmentDto>())
                .OrderBy(x => x.AssignedAtUtc)
                .ThenBy(x => x.DueAtUtc ?? DateTimeOffset.MaxValue)
                .ThenBy(x => x.ScenarioId, StringComparer.OrdinalIgnoreCase)
                .ToList();
        }
        catch
        {
            ResetAssignmentState();
            SetMessage("Could not load assignments from the API", false);
        }
    }

    private void ResetAssignmentState()
    {
        _assignments = new List<ClassAssignmentDto>();
        _assignmentName = "";
        _assignmentScenarioId = "";
        _assignmentAssignDate = "";
        _assignmentAssignHour = "";
        _assignmentAssignMinute = "";
        _assignmentAssignPeriod = DefaultAssignPeriod;
        _assignmentDate = "";
        _assignmentDueHour = "";
        _assignmentDueMinute = "";
        _assignmentDuePeriod = DefaultDuePeriod;
        _isAddAssignmentModalOpen = false;
        _isAddTeacherModalOpen = false;
        _isAddStudentModalOpen = false;
        CloseEditAssignmentModal();
    }

    private async Task CreateAssignmentAsync()
    {
        if (_selectedClass is null)
        {
            SetAssignmentModalMessage("Select a class first");
            return;
        }

        var assignmentName = (_assignmentName ?? "").Trim();
        var assignmentScenarioId = (_assignmentScenarioId ?? "").Trim();
        var assignmentAssignDate = (_assignmentAssignDate ?? "").Trim();
        var assignmentAssignTime = string.IsNullOrWhiteSpace(assignmentAssignDate)
            ? ""
            : BuildTwelveHourTimeText(_assignmentAssignHour, _assignmentAssignMinute, _assignmentAssignPeriod);
        var assignmentDate = (_assignmentDate ?? "").Trim();
        var assignmentTime = string.IsNullOrWhiteSpace(assignmentDate)
            ? ""
            : BuildTwelveHourTimeText(_assignmentDueHour, _assignmentDueMinute, _assignmentDuePeriod);

        if (string.IsNullOrWhiteSpace(assignmentName))
        {
            SetAssignmentModalMessage("Enter an assignment name");
            return;
        }

        if (string.IsNullOrWhiteSpace(assignmentScenarioId))
        {
            SetAssignmentModalMessage("Select a scenario to assign");
            return;
        }

        if (!TryBuildOptionalCstDateTimeUtc(
            assignmentAssignDate,
            assignmentAssignTime,
            defaultTime: new TimeOnly(0, 0),
            missingDateMessage: "Select an assign date when entering an assign time",
            invalidDateMessage: "Invalid assign date",
            invalidTimeMessage: "Invalid assign time. Use h:mm AM/PM.",
            out var assignedAtUtc,
            out var assignError))
        {
            SetAssignmentModalMessage(assignError);
            return;
        }

        if (!TryBuildOptionalCstDateTimeUtc(
            assignmentDate,
            assignmentTime,
            defaultTime: new TimeOnly(23, 59),
            missingDateMessage: "Select a due date when entering a due time",
            invalidDateMessage: "Invalid due date",
            invalidTimeMessage: "Invalid due time. Use h:mm AM/PM.",
            out var dueAtUtc,
            out var dueError))
        {
            SetAssignmentModalMessage(dueError);
            return;
        }

        if (assignedAtUtc.HasValue && dueAtUtc.HasValue && dueAtUtc.Value < assignedAtUtc.Value)
        {
            SetAssignmentModalMessage("Due date must be after the assign date");
            return;
        }

        try
        {
            ClearAssignmentModalMessage();
            var resp = await Api.Http.PostAsJsonAsync($"api/classes/{_selectedClass.Id}/assignments", new CreateAssignmentRequest
            {
                Name = assignmentName,
                ScenarioId = assignmentScenarioId,
                AssignedAtUtc = assignedAtUtc,
                DueAtUtc = dueAtUtc
            });

            if (!resp.IsSuccessStatusCode)
            {
                var body = await resp.Content.ReadAsStringAsync();
                SetAssignmentModalMessage($"Assign failed {((int)resp.StatusCode)} {body}");

                return;
            }

            _assignmentName = "";
            _assignmentAssignDate = "";
            _assignmentAssignHour = "";
            _assignmentAssignMinute = "";
            _assignmentAssignPeriod = DefaultAssignPeriod;
            _assignmentDate = "";
            _assignmentDueHour = "";
            _assignmentDueMinute = "";
            _assignmentDuePeriod = DefaultDuePeriod;
            _isAddAssignmentModalOpen = false;
            SetMessage(assignedAtUtc.HasValue ? "Assignment scheduled" : "Scenario assigned", true);
            await ReloadAssignmentsAsync();
        }
        catch
        {
            SetAssignmentModalMessage("Could not create assignment");
        }
    }

    private async Task OpenEditAssignmentModalAsync(ClassAssignmentDto assignment)
    {
        if (_selectedClass is null)
        {
            return;
        }

        await ReloadAvailableScenariosAsync();
        ClearAssignmentModalMessage();
        _editingAssignmentId = assignment.Id;
        _editAssignmentName = assignment.Name;
        _editAssignmentScenarioId = assignment.ScenarioId;

        var assignCst = assignment.AssignedAtUtc.ToOffset(CstOffset);
        _editAssignmentAssignDate = assignCst.ToString("yyyy-MM-dd", CultureInfo.InvariantCulture);
        _editAssignmentAssignHour = assignCst.ToString("%h", CultureInfo.InvariantCulture);
        _editAssignmentAssignMinute = assignCst.ToString("mm", CultureInfo.InvariantCulture);
        _editAssignmentAssignPeriod = assignCst.ToString("tt", CultureInfo.InvariantCulture);

        if (assignment.DueAtUtc.HasValue)
        {
            var dueCst = assignment.DueAtUtc.Value.ToOffset(CstOffset);
            _editAssignmentDueDate = dueCst.ToString("yyyy-MM-dd", CultureInfo.InvariantCulture);
            _editAssignmentDueHour = dueCst.ToString("%h", CultureInfo.InvariantCulture);
            _editAssignmentDueMinute = dueCst.ToString("mm", CultureInfo.InvariantCulture);
            _editAssignmentDuePeriod = dueCst.ToString("tt", CultureInfo.InvariantCulture);
        }
        else
        {
            _editAssignmentDueDate = "";
            _editAssignmentDueHour = "";
            _editAssignmentDueMinute = "";
            _editAssignmentDuePeriod = DefaultDuePeriod;
        }

        _isAddAssignmentModalOpen = false;
        _isAddClassModalOpen = false;
        _isAddTeacherModalOpen = false;
        _isAddStudentModalOpen = false;
    }

    private void CloseEditAssignmentModal()
    {
        ClearAssignmentModalMessage();
        _editingAssignmentId = null;
        _editAssignmentName = "";
        _editAssignmentScenarioId = "";
        _editAssignmentAssignDate = "";
        _editAssignmentAssignHour = "";
        _editAssignmentAssignMinute = "";
        _editAssignmentAssignPeriod = DefaultAssignPeriod;
        _editAssignmentDueDate = "";
        _editAssignmentDueHour = "";
        _editAssignmentDueMinute = "";
        _editAssignmentDuePeriod = DefaultDuePeriod;
        _isSavingAssignment = false;
    }

    private async Task SaveAssignmentAsync(int assignmentId)
    {
        if (_selectedClass is null)
        {
            SetAssignmentModalMessage("Select a class first");
            return;
        }

        var assignmentName = (_editAssignmentName ?? "").Trim();
        var scenarioId = (_editAssignmentScenarioId ?? "").Trim();
        var assignDate = (_editAssignmentAssignDate ?? "").Trim();
        var assignTime = string.IsNullOrWhiteSpace(assignDate)
            ? ""
            : BuildTwelveHourTimeText(_editAssignmentAssignHour, _editAssignmentAssignMinute, _editAssignmentAssignPeriod);
        var dueDate = (_editAssignmentDueDate ?? "").Trim();
        var dueTime = string.IsNullOrWhiteSpace(dueDate)
            ? ""
            : BuildTwelveHourTimeText(_editAssignmentDueHour, _editAssignmentDueMinute, _editAssignmentDuePeriod);

        if (string.IsNullOrWhiteSpace(assignmentName))
        {
            SetAssignmentModalMessage("Enter an assignment name");
            return;
        }

        if (string.IsNullOrWhiteSpace(scenarioId))
        {
            SetAssignmentModalMessage("Select a scenario to assign");
            return;
        }

        if (!TryBuildOptionalCstDateTimeUtc(
            assignDate,
            assignTime,
            defaultTime: new TimeOnly(0, 0),
            missingDateMessage: "Select an assign date when entering an assign time",
            invalidDateMessage: "Invalid assign date",
            invalidTimeMessage: "Invalid assign time. Use h:mm AM/PM.",
            out var assignedAtUtc,
            out var assignError))
        {
            SetAssignmentModalMessage(assignError);
            return;
        }

        if (!TryBuildOptionalCstDateTimeUtc(
            dueDate,
            dueTime,
            defaultTime: new TimeOnly(23, 59),
            missingDateMessage: "Select a due date when entering a due time",
            invalidDateMessage: "Invalid due date",
            invalidTimeMessage: "Invalid due time. Use h:mm AM/PM.",
            out var dueAtUtc,
            out var dueError))
        {
            SetAssignmentModalMessage(dueError);
            return;
        }

        if (assignedAtUtc.HasValue && dueAtUtc.HasValue && dueAtUtc.Value < assignedAtUtc.Value)
        {
            SetAssignmentModalMessage("Due date must be after the assign date");
            return;
        }

        _isSavingAssignment = true;
        try
        {
            ClearAssignmentModalMessage();
            var resp = await Api.Http.PutAsJsonAsync(
                $"api/classes/{_selectedClass.Id}/assignments/{assignmentId}",
                new UpdateAssignmentRequest
                {
                    Name = assignmentName,
                    ScenarioId = scenarioId,
                    AssignedAtUtc = assignedAtUtc,
                    DueAtUtc = dueAtUtc
                });

            if (!resp.IsSuccessStatusCode)
            {
                var body = await resp.Content.ReadAsStringAsync();
                SetAssignmentModalMessage($"Update assignment failed {((int)resp.StatusCode)} {body}");
                return;
            }

            SetMessage("Assignment updated", true);
            CloseEditAssignmentModal();
            await ReloadAssignmentsAsync();
        }
        catch
        {
            SetAssignmentModalMessage("Could not update assignment");
        }
        finally
        {
            _isSavingAssignment = false;
        }
    }

    private static string FormatAssignedAtCst(DateTimeOffset assignedAtUtc)
    {
        return $"{assignedAtUtc.ToOffset(CstOffset).ToString("MM/dd/yyyy h:mm tt", CultureInfo.InvariantCulture)} CST";
    }

    private static string FormatDueAtCst(DateTimeOffset? dueAtUtc)
    {
        if (!dueAtUtc.HasValue)
        {
            return "No due date";
        }

        return $"{dueAtUtc.Value.ToOffset(CstOffset).ToString("MM/dd/yyyy h:mm tt", CultureInfo.InvariantCulture)} CST";
    }

    private static string BuildTwelveHourTimeText(string hour, string minute, string period)
    {
        var normalizedHour = int.TryParse(hour, out var parsedHour)
            ? Math.Clamp(parsedHour, 1, 12).ToString(CultureInfo.InvariantCulture)
            : DefaultDueHour;

        var normalizedMinute = int.TryParse(minute, out var parsedMinute)
            ? Math.Clamp(parsedMinute, 0, 59).ToString("00", CultureInfo.InvariantCulture)
            : DefaultDueMinute;

        var normalizedPeriod = string.Equals(period, "AM", StringComparison.OrdinalIgnoreCase) ? "AM" : "PM";
        return $"{normalizedHour}:{normalizedMinute} {normalizedPeriod}";
    }

    private static string NormalizeTimePartInput(string? rawValue)
    {
        var digitsOnly = new string((rawValue ?? "").Where(char.IsDigit).Take(2).ToArray());
        return digitsOnly;
    }

    private static bool TryBuildOptionalCstDateTimeUtc(
        string dateText,
        string timeText,
        TimeOnly defaultTime,
        string missingDateMessage,
        string invalidDateMessage,
        string invalidTimeMessage,
        out DateTimeOffset? valueUtc,
        out string error)
    {
        valueUtc = null;
        error = "";

        if (string.IsNullOrWhiteSpace(dateText) && string.IsNullOrWhiteSpace(timeText))
        {
            return true;
        }

        if (string.IsNullOrWhiteSpace(dateText))
        {
            error = missingDateMessage;
            return false;
        }

        if (!DateOnly.TryParse(dateText, CultureInfo.InvariantCulture, DateTimeStyles.None, out var parsedDate))
        {
            error = invalidDateMessage;
            return false;
        }

        TimeOnly parsedTime;
        if (string.IsNullOrWhiteSpace(timeText))
        {
            parsedTime = defaultTime;
        }
        else if (!TryParseTwelveHourTime(timeText, out parsedTime))
        {
            error = invalidTimeMessage;
            return false;
        }

        var valueCst = new DateTimeOffset(parsedDate.ToDateTime(parsedTime), CstOffset);
        valueUtc = valueCst.ToUniversalTime();
        return true;
    }

    private static bool TryParseTwelveHourTime(string input, out TimeOnly time)
    {
        var value = (input ?? "").Trim();
        var formats = new[] { "h:mm tt", "hh:mm tt", "h:mmtt", "hh:mmtt" };
        return TimeOnly.TryParseExact(value, formats, CultureInfo.InvariantCulture, DateTimeStyles.AllowWhiteSpaces, out time);
    }

    private async Task DeleteAssignmentAsync(int assignmentId)
    {
        if (_selectedClass is null) return;

        try
        {
            var resp = await Api.Http.DeleteAsync($"api/classes/{_selectedClass.Id}/assignments/{assignmentId}");
            if (!resp.IsSuccessStatusCode)
            {
                var body = await resp.Content.ReadAsStringAsync();
                SetMessage($"Delete assignment failed {((int)resp.StatusCode)} {body}", false);
                return;
            }

            SetMessage("Assignment deleted", true);
            await ReloadAssignmentsAsync();
        }
        catch
        {
            SetMessage("Could not delete assignment", false);
        }
    }

    private async Task CreateClassAsync()
    {
        var name = _newClassName.Trim();
        if (string.IsNullOrWhiteSpace(name))
        {
            SetMessage("Enter a class name", false);
            return;
        }

        try
        {
            var resp = await Api.Http.PostAsJsonAsync("api/classes", new CreateClassRequest { Name = name });

            if (!resp.IsSuccessStatusCode)
            {
                var body = await resp.Content.ReadAsStringAsync();
                SetMessage($"Create failed {((int)resp.StatusCode)} {body}", false);
                return;
            }

            var created = await resp.Content.ReadFromJsonAsync<ClassRosterDto>();
            _newClassName = "";
            _isAddClassModalOpen = false;
            _selectedClassId = created?.Id ?? 0;
            SetMessage("Class created", true);

            await ReloadClassesAsync();
            await ReloadAvailableScenariosAsync();
            await ReloadAssignmentsAsync();
        }
        catch
        {
            SetMessage("Could not create class", false);
        }
    }

    private async Task DeleteSelectedClassAsync()
    {
        _menuOpen = false;

        if (_selectedClassId == 0)
        {
            SetMessage("Select a class first", false);
            return;
        }

        try
        {
            var resp = await Api.Http.DeleteAsync($"api/classes/{_selectedClassId}");
            if (!resp.IsSuccessStatusCode)
            {
                var body = await resp.Content.ReadAsStringAsync();
                SetMessage($"Delete failed {((int)resp.StatusCode)} {body}", false);
                return;
            }

            _selectedClassId = 0;
            _selectedClass = null;
            _emailToAdd = "";
            _teacherEmailToAdd = "";
            _allScenarioIds = new List<string>();
            _isAddClassModalOpen = false;
            _isAddTeacherModalOpen = false;
            _isAddStudentModalOpen = false;
            _isAddAssignmentModalOpen = false;
            ResetAssignmentState();

            SetMessage("Class deleted", true);

            await ReloadClassesAsync();
            if (_classes.Count > 0)
            {
                _selectedClassId = _classes[0].Id;
                _selectedClass = _classes[0];
                await ReloadAvailableScenariosAsync();
                await ReloadAssignmentsAsync();
            }
        }
        catch
        {
            SetMessage("Could not delete class", false);
        }
    }

    private async Task OnSelectedClassChanged(ChangeEventArgs e)
    {
        _selectedClassId = int.TryParse(e.Value?.ToString(), out var id) ? id : 0;
        _emailToAdd = "";
        _teacherEmailToAdd = "";
        _menuOpen = false;
        _isAddClassModalOpen = false;
        _isAddTeacherModalOpen = false;
        _isAddStudentModalOpen = false;
        _isAddAssignmentModalOpen = false;
        CloseEditAssignmentModal();

        await ReloadClassesAsync();
        await ReloadAvailableScenariosAsync();
        await ReloadAssignmentsAsync();
    }

    private async Task AddStudentAsync()
    {
        if (_selectedClass is null)
        {
            SetMessage("Select a class first", false);
            return;
        }

        var email = _emailToAdd.Trim().ToLowerInvariant();
        if (!LooksLikeEmail(email))
        {
            SetMessage("Enter a valid email", false);
            return;
        }

        try
        {
            var resp = await Api.Http.PostAsJsonAsync($"api/classes/{_selectedClass.Id}/students", new StudentRequest { Email = email });
            if (!resp.IsSuccessStatusCode)
            {
                var body = await resp.Content.ReadAsStringAsync();
                SetMessage($"Add failed {((int)resp.StatusCode)} {body}", false);
                return;
            }

            _emailToAdd = "";
            _isAddStudentModalOpen = false;
            SetMessage("Student added", true);
            await ReloadClassesAsync();
            await ReloadAssignmentsAsync();
        }
        catch
        {
            SetMessage("Could not add student", false);
        }
    }

    private async Task AddTeacherAsync()
    {
        if (_selectedClass is null)
        {
            SetMessage("Select a class first", false);
            return;
        }

        var email = _teacherEmailToAdd.Trim().ToLowerInvariant();
        if (!LooksLikeEmail(email))
        {
            SetMessage("Enter a valid teacher email", false);
            return;
        }

        try
        {
            var resp = await Api.Http.PostAsJsonAsync($"api/classes/{_selectedClass.Id}/teachers", new StudentRequest { Email = email });
            if (!resp.IsSuccessStatusCode)
            {
                var body = await resp.Content.ReadAsStringAsync();
                SetMessage($"Add teacher failed {((int)resp.StatusCode)} {body}", false);
                return;
            }

            _teacherEmailToAdd = "";
            _isAddTeacherModalOpen = false;
            SetMessage("Teacher added", true);
            await ReloadClassesAsync();
        }
        catch
        {
            SetMessage("Could not add teacher", false);
        }
    }

    private async Task RemoveTeacherAsync(string email)
    {
        if (_selectedClass is null) return;

        try
        {
            var req = new HttpRequestMessage(HttpMethod.Delete, $"api/classes/{_selectedClass.Id}/teachers")
            {
                Content = JsonContent.Create(new StudentRequest { Email = email })
            };

            var resp = await Api.Http.SendAsync(req);
            if (!resp.IsSuccessStatusCode)
            {
                var body = await resp.Content.ReadAsStringAsync();
                SetMessage($"Remove teacher failed {((int)resp.StatusCode)} {body}", false);
                return;
            }

            SetMessage("Teacher removed", true);
            await ReloadClassesAsync();
        }
        catch
        {
            SetMessage("Could not remove teacher", false);
        }
    }

    private bool IsCurrentTeacher(string email)
    {
        return string.Equals((email ?? "").Trim(), _currentEmail, StringComparison.OrdinalIgnoreCase);
    }

    private async Task RemoveStudentAsync(string email)
    {
        if (_selectedClass is null) return;

        try
        {
            var req = new HttpRequestMessage(HttpMethod.Delete, $"api/classes/{_selectedClass.Id}/students")
            {
                Content = JsonContent.Create(new StudentRequest { Email = email })
            };

            var resp = await Api.Http.SendAsync(req);
            if (!resp.IsSuccessStatusCode)
            {
                var body = await resp.Content.ReadAsStringAsync();
                SetMessage($"Remove failed {((int)resp.StatusCode)} {body}", false);
                return;
            }

            SetMessage("Student removed", true);
            await ReloadClassesAsync();
            await ReloadAssignmentsAsync();
        }
        catch
        {
            SetMessage("Could not remove student", false);
        }
    }

    private static bool LooksLikeEmail(string value)
    {
        try
        {
            var addr = new System.Net.Mail.MailAddress(value);
            return string.Equals(addr.Address, value, StringComparison.OrdinalIgnoreCase);
        }
        catch
        {
            return false;
        }
    }

    private sealed class ClassRosterDto
    {
        public int Id { get; set; }
        public string Name { get; set; } = "";
        public List<string> Teachers { get; set; } = new();
        public List<string> Students { get; set; } = new();
    }

    private sealed class AssignmentSubmissionDto
    {
        public string StudentEmail { get; set; } = "";
        public DateTimeOffset? SubmittedAtUtc { get; set; }
        public string SubmissionText { get; set; } = "";
        public decimal? Grade { get; set; }
        public string GradeFeedback { get; set; } = "";
        public DateTimeOffset? GradedAtUtc { get; set; }
        public string GradedByEmail { get; set; } = "";
    }

    private sealed class ClassAssignmentDto
    {
        public int Id { get; set; }
        public string Name { get; set; } = "";
        public string ScenarioId { get; set; } = "";
        public DateTimeOffset AssignedAtUtc { get; set; }
        public DateTimeOffset? DueAtUtc { get; set; }
        public List<AssignmentSubmissionDto> Submissions { get; set; } = new();
    }

    private sealed class CreateClassRequest
    {
        public string Name { get; set; } = "";
    }

    private sealed class StudentRequest
    {
        public string Email { get; set; } = "";
    }

    private sealed class CreateAssignmentRequest
    {
        public string Name { get; set; } = "";
        public string ScenarioId { get; set; } = "";
        public DateTimeOffset? AssignedAtUtc { get; set; }
        public DateTimeOffset? DueAtUtc { get; set; }
    }

    private sealed class UpdateAssignmentDueRequest
    {
        public DateTimeOffset? DueAtUtc { get; set; }
    }

    private sealed class UpdateAssignmentRequest
    {
        public string Name { get; set; } = "";
        public string ScenarioId { get; set; } = "";
        public DateTimeOffset? AssignedAtUtc { get; set; }
        public DateTimeOffset? DueAtUtc { get; set; }
    }

}
